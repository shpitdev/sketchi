#!/usr/bin/env bash
#MISE description="Lint plugin package with Ultracite"

set -euo pipefail

ROOT="$(git rev-parse --show-toplevel)"
cd "$ROOT"

bun run ultracite check packages/opencode-excalidraw
