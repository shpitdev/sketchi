#!/usr/bin/env bash
#MISE description="Build and smoke-test the plugin artifact"
#MISE depends=["build"]

set -euo pipefail

ROOT="$(git rev-parse --show-toplevel)"
cd "$ROOT"

bun run --cwd packages/opencode-excalidraw smoke
