{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "diagram-exporter",
  "projectType": "library",
  "sourceRoot": "packages/diagram-exporter/src",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -b packages/diagram-exporter/tsconfig.lib.json"
      },
      "outputs": ["{projectRoot}/dist"]
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run --config vitest.config.ts",
        "cwd": "packages/diagram-exporter"
      },
      "outputs": ["{workspaceRoot}/coverage/packages/diagram-exporter"]
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -b packages/diagram-exporter/tsconfig.json --pretty false"
      },
      "outputs": ["{projectRoot}/dist/typecheck"]
    },
    "typecheck-tsgo": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsgo -p packages/diagram-exporter/tsconfig.json --noEmit"
      }
    }
  },
  "tags": ["scope:diagram", "type:exporter"]
}
