import type { Meta, StoryObj } from "@storybook/react-vite";

import { <%= fixtureName %> } from "@sketchi/diagram-core";

import { GenerationWorkspace } from "../components/generation-workspace/index.js";
import "../styles.css";

const meta = {
  title: "Diagram Types/<%= className %>",
  component: GenerationWorkspace,
  args: {
    diagram: <%= fixtureName %>,
    status: "ready"
  }
} satisfies Meta<typeof GenerationWorkspace>;

export default meta;

type Story = StoryObj<typeof meta>;

export const Ready: Story = {};
