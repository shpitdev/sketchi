/**
 * The Sketchi web home — "the true home-home". Every brand mark in the Studio
 * app links here so clicking the logo always returns to sketchi.app.
 *
 * workers.dev is the live surface today (the sketchi.app domains are attached
 * manually), so this defaults to the deployed web worker URL. Update here when
 * the custom domain is attached.
 */
export const SKETCHI_WEB_HOME_URL = "https://sketchi-web.dimethyl.workers.dev";
