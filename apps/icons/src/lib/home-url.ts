/**
 * The Sketchi web home — "the true home-home". The icon library's brand mark
 * links here so clicking the logo always returns to sketchi.app.
 *
 * workers.dev is the live surface today (the sketchi.app domains are attached
 * manually), so this defaults to the deployed web worker URL.
 */
export const SKETCHI_WEB_HOME_URL = "https://sketchi-web.dimethyl.workers.dev";
